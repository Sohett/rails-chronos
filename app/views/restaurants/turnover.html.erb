<% Groupdate.time_zone = "Brussels" %>
<div class="container">
  <h3 class="text-center">Statistics: <%= @restaurant.name %> </h3>
  <div class="box">

  <div class="chart">
    <h4 class="text-center">Turnover (€/day)</h4>
    <%= column_chart @restaurant.orders.group_by_day(:time).sum("amount_cents / 100") %>
  </div>

  <div class="text-center">
    <%= link_to 'Dashboard', restaurant_dashboard_path(@restaurant), class: "btn btn-back" %>
  </div>
  </div>
</div>
