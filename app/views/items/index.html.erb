<%= render 'shared/banner'  %>

<div class="container">
  <h2>Digital menu</h2>
  <ul>
    <% @items.each do |item|%>
    <li><%= item.name %></li>
    <li><%= item.description %></li>
    <li><%= item.price  %>€</li>
    <li><%= item.category %></li>
    <% end %>
  </ul>
</div>
